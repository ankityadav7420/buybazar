fastapi
uvicorn[standard]
sqlalchemy
asyncpg
pydantic
psycopg2-binary
greenlet
pytest
pytest-cov
debugpy
# pip install -r requirements.txt
# source venv/bin/activate
#  uvicorn app.main:app --reload  
#  docker-compose up --build

#  DROPPING THE DATABASE then build again
# docker-compose down -v
# docker-compose up --build

# docker-compose down
# docker-compose up --build


#  Run test:  venv/bin/pytest -v --cov=app tests/
# */
